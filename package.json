{
  "name": "ledit",
  "version": "0.1.0",
  "description": "A LED matrix infotainment system",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --shard=1/2",
    "dev": "vite --host 0.0.0.0",
    "build": "npm run icons && vite build",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^24.0.0",
    "sharp": "^0.35.0",
    "typescript": "^7.0.0",
    "vite": "^8.0.0"
  }
}
