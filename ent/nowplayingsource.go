// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/nowplayingsource"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// NowPlayingSource is the model entity for the NowPlayingSource schema.
type NowPlayingSource struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider nowplayingsource.Provider `json:"provider,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Token holds the value of the "token" field.
	Token string `json:"token,omitempty"`
	// Username holds the value of the "username" field.
	Username string `json:"username,omitempty"`
	// ShowAlbumArt holds the value of the "show_album_art" field.
	ShowAlbumArt                         bool `json:"show_album_art,omitempty"`
	general_settings_now_playing_sources *int
	selectValues                         sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NowPlayingSource) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case nowplayingsource.FieldShowAlbumArt:
			values[i] = new(sql.NullBool)
		case nowplayingsource.FieldID:
			values[i] = new(sql.NullInt64)
		case nowplayingsource.FieldName, nowplayingsource.FieldProvider, nowplayingsource.FieldURL, nowplayingsource.FieldToken, nowplayingsource.FieldUsername:
			values[i] = new(sql.NullString)
		case nowplayingsource.ForeignKeys[0]: // general_settings_now_playing_sources
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NowPlayingSource fields.
func (_m *NowPlayingSource) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case nowplayingsource.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case nowplayingsource.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case nowplayingsource.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = nowplayingsource.Provider(value.String)
			}
		case nowplayingsource.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case nowplayingsource.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case nowplayingsource.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				_m.Username = value.String
			}
		case nowplayingsource.FieldShowAlbumArt:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field show_album_art", values[i])
			} else if value.Valid {
				_m.ShowAlbumArt = value.Bool
			}
		case nowplayingsource.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_now_playing_sources", value)
			} else if value.Valid {
				_m.general_settings_now_playing_sources = new(int)
				*_m.general_settings_now_playing_sources = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NowPlayingSource.
// This includes values selected through modifiers, order, etc.
func (_m *NowPlayingSource) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this NowPlayingSource.
// Note that you need to call NowPlayingSource.Unwrap() before calling this method if this NowPlayingSource
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *NowPlayingSource) Update() *NowPlayingSourceUpdateOne {
	return NewNowPlayingSourceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the NowPlayingSource entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *NowPlayingSource) Unwrap() *NowPlayingSource {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: NowPlayingSource is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *NowPlayingSource) String() string {
	var builder strings.Builder
	builder.WriteString("NowPlayingSource(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("provider=")
	builder.WriteString(fmt.Sprintf("%v", _m.Provider))
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteString(", ")
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("username=")
	builder.WriteString(_m.Username)
	builder.WriteString(", ")
	builder.WriteString("show_album_art=")
	builder.WriteString(fmt.Sprintf("%v", _m.ShowAlbumArt))
	builder.WriteByte(')')
	return builder.String()
}

// NowPlayingSources is a parsable slice of NowPlayingSource.
type NowPlayingSources []*NowPlayingSource
