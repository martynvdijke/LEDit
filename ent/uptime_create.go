// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ledit/ent/uptime"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UptimeCreate is the builder for creating a Uptime entity.
type UptimeCreate struct {
	config
	mutation *UptimeMutation
	hooks    []Hook
}

// SetURL sets the "url" field.
func (_c *UptimeCreate) SetURL(v string) *UptimeCreate {
	_c.mutation.SetURL(v)
	return _c
}

// SetConfig sets the "config" field.
func (_c *UptimeCreate) SetConfig(v string) *UptimeCreate {
	_c.mutation.SetConfig(v)
	return _c
}

// SetNillableConfig sets the "config" field if the given value is not nil.
func (_c *UptimeCreate) SetNillableConfig(v *string) *UptimeCreate {
	if v != nil {
		_c.SetConfig(*v)
	}
	return _c
}

// Mutation returns the UptimeMutation object of the builder.
func (_c *UptimeCreate) Mutation() *UptimeMutation {
	return _c.mutation
}

// Save creates the Uptime in the database.
func (_c *UptimeCreate) Save(ctx context.Context) (*Uptime, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *UptimeCreate) SaveX(ctx context.Context) *Uptime {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *UptimeCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *UptimeCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *UptimeCreate) defaults() {
	if _, ok := _c.mutation.Config(); !ok {
		v := uptime.DefaultConfig
		_c.mutation.SetConfig(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *UptimeCreate) check() error {
	if _, ok := _c.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "Uptime.url"`)}
	}
	if _, ok := _c.mutation.Config(); !ok {
		return &ValidationError{Name: "config", err: errors.New(`ent: missing required field "Uptime.config"`)}
	}
	return nil
}

func (_c *UptimeCreate) sqlSave(ctx context.Context) (*Uptime, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *UptimeCreate) createSpec() (*Uptime, *sqlgraph.CreateSpec) {
	var (
		_node = &Uptime{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(uptime.Table, sqlgraph.NewFieldSpec(uptime.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.URL(); ok {
		_spec.SetField(uptime.FieldURL, field.TypeString, value)
		_node.URL = value
	}
	if value, ok := _c.mutation.Config(); ok {
		_spec.SetField(uptime.FieldConfig, field.TypeString, value)
		_node.Config = value
	}
	return _node, _spec
}

// UptimeCreateBulk is the builder for creating many Uptime entities in bulk.
type UptimeCreateBulk struct {
	config
	err      error
	builders []*UptimeCreate
}

// Save creates the Uptime entities in the database.
func (_c *UptimeCreateBulk) Save(ctx context.Context) ([]*Uptime, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*Uptime, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*UptimeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *UptimeCreateBulk) SaveX(ctx context.Context) []*Uptime {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *UptimeCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *UptimeCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
