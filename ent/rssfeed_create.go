// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ledit/ent/rssfeed"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RssFeedCreate is the builder for creating a RssFeed entity.
type RssFeedCreate struct {
	config
	mutation *RssFeedMutation
	hooks    []Hook
}

// SetURL sets the "url" field.
func (_c *RssFeedCreate) SetURL(v string) *RssFeedCreate {
	_c.mutation.SetURL(v)
	return _c
}

// SetName sets the "name" field.
func (_c *RssFeedCreate) SetName(v string) *RssFeedCreate {
	_c.mutation.SetName(v)
	return _c
}

// SetNillableName sets the "name" field if the given value is not nil.
func (_c *RssFeedCreate) SetNillableName(v *string) *RssFeedCreate {
	if v != nil {
		_c.SetName(*v)
	}
	return _c
}

// Mutation returns the RssFeedMutation object of the builder.
func (_c *RssFeedCreate) Mutation() *RssFeedMutation {
	return _c.mutation
}

// Save creates the RssFeed in the database.
func (_c *RssFeedCreate) Save(ctx context.Context) (*RssFeed, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *RssFeedCreate) SaveX(ctx context.Context) *RssFeed {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *RssFeedCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *RssFeedCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *RssFeedCreate) defaults() {
	if _, ok := _c.mutation.Name(); !ok {
		v := rssfeed.DefaultName
		_c.mutation.SetName(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *RssFeedCreate) check() error {
	if _, ok := _c.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "RssFeed.url"`)}
	}
	if _, ok := _c.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "RssFeed.name"`)}
	}
	return nil
}

func (_c *RssFeedCreate) sqlSave(ctx context.Context) (*RssFeed, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *RssFeedCreate) createSpec() (*RssFeed, *sqlgraph.CreateSpec) {
	var (
		_node = &RssFeed{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(rssfeed.Table, sqlgraph.NewFieldSpec(rssfeed.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.URL(); ok {
		_spec.SetField(rssfeed.FieldURL, field.TypeString, value)
		_node.URL = value
	}
	if value, ok := _c.mutation.Name(); ok {
		_spec.SetField(rssfeed.FieldName, field.TypeString, value)
		_node.Name = value
	}
	return _node, _spec
}

// RssFeedCreateBulk is the builder for creating many RssFeed entities in bulk.
type RssFeedCreateBulk struct {
	config
	err      error
	builders []*RssFeedCreate
}

// Save creates the RssFeed entities in the database.
func (_c *RssFeedCreateBulk) Save(ctx context.Context) ([]*RssFeed, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*RssFeed, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RssFeedMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *RssFeedCreateBulk) SaveX(ctx context.Context) []*RssFeed {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *RssFeedCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *RssFeedCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
