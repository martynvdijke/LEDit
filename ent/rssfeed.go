// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/rssfeed"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RssFeed is the model entity for the RssFeed schema.
type RssFeed struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Name holds the value of the "name" field.
	Name                       string `json:"name,omitempty"`
	general_settings_rss_feeds *int
	selectValues               sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RssFeed) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case rssfeed.FieldID:
			values[i] = new(sql.NullInt64)
		case rssfeed.FieldURL, rssfeed.FieldName:
			values[i] = new(sql.NullString)
		case rssfeed.ForeignKeys[0]: // general_settings_rss_feeds
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RssFeed fields.
func (_m *RssFeed) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case rssfeed.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case rssfeed.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case rssfeed.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case rssfeed.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_rss_feeds", value)
			} else if value.Valid {
				_m.general_settings_rss_feeds = new(int)
				*_m.general_settings_rss_feeds = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RssFeed.
// This includes values selected through modifiers, order, etc.
func (_m *RssFeed) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RssFeed.
// Note that you need to call RssFeed.Unwrap() before calling this method if this RssFeed
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RssFeed) Update() *RssFeedUpdateOne {
	return NewRssFeedClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RssFeed entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RssFeed) Unwrap() *RssFeed {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RssFeed is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RssFeed) String() string {
	var builder strings.Builder
	builder.WriteString("RssFeed(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteByte(')')
	return builder.String()
}

// RssFeeds is a parsable slice of RssFeed.
type RssFeeds []*RssFeed
