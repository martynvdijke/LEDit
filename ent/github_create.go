// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ledit/ent/github"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GitHubCreate is the builder for creating a GitHub entity.
type GitHubCreate struct {
	config
	mutation *GitHubMutation
	hooks    []Hook
}

// SetToken sets the "token" field.
func (_c *GitHubCreate) SetToken(v string) *GitHubCreate {
	_c.mutation.SetToken(v)
	return _c
}

// SetNillableToken sets the "token" field if the given value is not nil.
func (_c *GitHubCreate) SetNillableToken(v *string) *GitHubCreate {
	if v != nil {
		_c.SetToken(*v)
	}
	return _c
}

// SetURL sets the "url" field.
func (_c *GitHubCreate) SetURL(v string) *GitHubCreate {
	_c.mutation.SetURL(v)
	return _c
}

// SetNillableURL sets the "url" field if the given value is not nil.
func (_c *GitHubCreate) SetNillableURL(v *string) *GitHubCreate {
	if v != nil {
		_c.SetURL(*v)
	}
	return _c
}

// Mutation returns the GitHubMutation object of the builder.
func (_c *GitHubCreate) Mutation() *GitHubMutation {
	return _c.mutation
}

// Save creates the GitHub in the database.
func (_c *GitHubCreate) Save(ctx context.Context) (*GitHub, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *GitHubCreate) SaveX(ctx context.Context) *GitHub {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *GitHubCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *GitHubCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *GitHubCreate) defaults() {
	if _, ok := _c.mutation.Token(); !ok {
		v := github.DefaultToken
		_c.mutation.SetToken(v)
	}
	if _, ok := _c.mutation.URL(); !ok {
		v := github.DefaultURL
		_c.mutation.SetURL(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *GitHubCreate) check() error {
	if _, ok := _c.mutation.Token(); !ok {
		return &ValidationError{Name: "token", err: errors.New(`ent: missing required field "GitHub.token"`)}
	}
	if _, ok := _c.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "GitHub.url"`)}
	}
	return nil
}

func (_c *GitHubCreate) sqlSave(ctx context.Context) (*GitHub, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *GitHubCreate) createSpec() (*GitHub, *sqlgraph.CreateSpec) {
	var (
		_node = &GitHub{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(github.Table, sqlgraph.NewFieldSpec(github.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.Token(); ok {
		_spec.SetField(github.FieldToken, field.TypeString, value)
		_node.Token = value
	}
	if value, ok := _c.mutation.URL(); ok {
		_spec.SetField(github.FieldURL, field.TypeString, value)
		_node.URL = value
	}
	return _node, _spec
}

// GitHubCreateBulk is the builder for creating many GitHub entities in bulk.
type GitHubCreateBulk struct {
	config
	err      error
	builders []*GitHubCreate
}

// Save creates the GitHub entities in the database.
func (_c *GitHubCreateBulk) Save(ctx context.Context) ([]*GitHub, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*GitHub, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GitHubMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *GitHubCreateBulk) SaveX(ctx context.Context) []*GitHub {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *GitHubCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *GitHubCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
