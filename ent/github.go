// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/github"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GitHub is the model entity for the GitHub schema.
type GitHub struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// GitHub repository identifier owner/repo
	Token string `json:"token,omitempty"`
	// GitHub API URL with %s for owner/repo
	URL                      string `json:"url,omitempty"`
	general_settings_githubs *int
	selectValues             sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GitHub) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case github.FieldID:
			values[i] = new(sql.NullInt64)
		case github.FieldToken, github.FieldURL:
			values[i] = new(sql.NullString)
		case github.ForeignKeys[0]: // general_settings_githubs
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GitHub fields.
func (_m *GitHub) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case github.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case github.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case github.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case github.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_githubs", value)
			} else if value.Valid {
				_m.general_settings_githubs = new(int)
				*_m.general_settings_githubs = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GitHub.
// This includes values selected through modifiers, order, etc.
func (_m *GitHub) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this GitHub.
// Note that you need to call GitHub.Unwrap() before calling this method if this GitHub
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GitHub) Update() *GitHubUpdateOne {
	return NewGitHubClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GitHub entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GitHub) Unwrap() *GitHub {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: GitHub is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GitHub) String() string {
	var builder strings.Builder
	builder.WriteString("GitHub(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteByte(')')
	return builder.String()
}

// GitHubs is a parsable slice of GitHub.
type GitHubs []*GitHub
