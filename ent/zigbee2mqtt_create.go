// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ledit/ent/zigbee2mqtt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// Zigbee2MQTTCreate is the builder for creating a Zigbee2MQTT entity.
type Zigbee2MQTTCreate struct {
	config
	mutation *Zigbee2MQTTMutation
	hooks    []Hook
}

// SetToken sets the "token" field.
func (_c *Zigbee2MQTTCreate) SetToken(v string) *Zigbee2MQTTCreate {
	_c.mutation.SetToken(v)
	return _c
}

// SetNillableToken sets the "token" field if the given value is not nil.
func (_c *Zigbee2MQTTCreate) SetNillableToken(v *string) *Zigbee2MQTTCreate {
	if v != nil {
		_c.SetToken(*v)
	}
	return _c
}

// SetURL sets the "url" field.
func (_c *Zigbee2MQTTCreate) SetURL(v string) *Zigbee2MQTTCreate {
	_c.mutation.SetURL(v)
	return _c
}

// SetNillableURL sets the "url" field if the given value is not nil.
func (_c *Zigbee2MQTTCreate) SetNillableURL(v *string) *Zigbee2MQTTCreate {
	if v != nil {
		_c.SetURL(*v)
	}
	return _c
}

// Mutation returns the Zigbee2MQTTMutation object of the builder.
func (_c *Zigbee2MQTTCreate) Mutation() *Zigbee2MQTTMutation {
	return _c.mutation
}

// Save creates the Zigbee2MQTT in the database.
func (_c *Zigbee2MQTTCreate) Save(ctx context.Context) (*Zigbee2MQTT, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *Zigbee2MQTTCreate) SaveX(ctx context.Context) *Zigbee2MQTT {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *Zigbee2MQTTCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *Zigbee2MQTTCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *Zigbee2MQTTCreate) defaults() {
	if _, ok := _c.mutation.Token(); !ok {
		v := zigbee2mqtt.DefaultToken
		_c.mutation.SetToken(v)
	}
	if _, ok := _c.mutation.URL(); !ok {
		v := zigbee2mqtt.DefaultURL
		_c.mutation.SetURL(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *Zigbee2MQTTCreate) check() error {
	if _, ok := _c.mutation.Token(); !ok {
		return &ValidationError{Name: "token", err: errors.New(`ent: missing required field "Zigbee2MQTT.token"`)}
	}
	if _, ok := _c.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "Zigbee2MQTT.url"`)}
	}
	return nil
}

func (_c *Zigbee2MQTTCreate) sqlSave(ctx context.Context) (*Zigbee2MQTT, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *Zigbee2MQTTCreate) createSpec() (*Zigbee2MQTT, *sqlgraph.CreateSpec) {
	var (
		_node = &Zigbee2MQTT{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(zigbee2mqtt.Table, sqlgraph.NewFieldSpec(zigbee2mqtt.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.Token(); ok {
		_spec.SetField(zigbee2mqtt.FieldToken, field.TypeString, value)
		_node.Token = value
	}
	if value, ok := _c.mutation.URL(); ok {
		_spec.SetField(zigbee2mqtt.FieldURL, field.TypeString, value)
		_node.URL = value
	}
	return _node, _spec
}

// Zigbee2MQTTCreateBulk is the builder for creating many Zigbee2MQTT entities in bulk.
type Zigbee2MQTTCreateBulk struct {
	config
	err      error
	builders []*Zigbee2MQTTCreate
}

// Save creates the Zigbee2MQTT entities in the database.
func (_c *Zigbee2MQTTCreateBulk) Save(ctx context.Context) ([]*Zigbee2MQTT, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*Zigbee2MQTT, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*Zigbee2MQTTMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *Zigbee2MQTTCreateBulk) SaveX(ctx context.Context) []*Zigbee2MQTT {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *Zigbee2MQTTCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *Zigbee2MQTTCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
