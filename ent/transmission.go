// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/transmission"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Transmission is the model entity for the Transmission schema.
type Transmission struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Transmission RPC password or token
	Token string `json:"token,omitempty"`
	// Transmission RPC URL
	URL                            string `json:"url,omitempty"`
	general_settings_transmissions *int
	selectValues                   sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Transmission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case transmission.FieldID:
			values[i] = new(sql.NullInt64)
		case transmission.FieldToken, transmission.FieldURL:
			values[i] = new(sql.NullString)
		case transmission.ForeignKeys[0]: // general_settings_transmissions
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Transmission fields.
func (_m *Transmission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case transmission.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case transmission.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case transmission.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case transmission.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_transmissions", value)
			} else if value.Valid {
				_m.general_settings_transmissions = new(int)
				*_m.general_settings_transmissions = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Transmission.
// This includes values selected through modifiers, order, etc.
func (_m *Transmission) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Transmission.
// Note that you need to call Transmission.Unwrap() before calling this method if this Transmission
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Transmission) Update() *TransmissionUpdateOne {
	return NewTransmissionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Transmission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Transmission) Unwrap() *Transmission {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Transmission is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Transmission) String() string {
	var builder strings.Builder
	builder.WriteString("Transmission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteByte(')')
	return builder.String()
}

// Transmissions is a parsable slice of Transmission.
type Transmissions []*Transmission
