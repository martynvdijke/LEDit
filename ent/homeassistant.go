// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/homeassistant"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// HomeAssistant is the model entity for the HomeAssistant schema.
type HomeAssistant struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Token holds the value of the "token" field.
	Token string `json:"token,omitempty"`
	// URL holds the value of the "url" field.
	URL                             string `json:"url,omitempty"`
	general_settings_home_assistant *int
	selectValues                    sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HomeAssistant) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case homeassistant.FieldID:
			values[i] = new(sql.NullInt64)
		case homeassistant.FieldToken, homeassistant.FieldURL:
			values[i] = new(sql.NullString)
		case homeassistant.ForeignKeys[0]: // general_settings_home_assistant
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HomeAssistant fields.
func (_m *HomeAssistant) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case homeassistant.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case homeassistant.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case homeassistant.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case homeassistant.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_home_assistant", value)
			} else if value.Valid {
				_m.general_settings_home_assistant = new(int)
				*_m.general_settings_home_assistant = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HomeAssistant.
// This includes values selected through modifiers, order, etc.
func (_m *HomeAssistant) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this HomeAssistant.
// Note that you need to call HomeAssistant.Unwrap() before calling this method if this HomeAssistant
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *HomeAssistant) Update() *HomeAssistantUpdateOne {
	return NewHomeAssistantClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the HomeAssistant entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *HomeAssistant) Unwrap() *HomeAssistant {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: HomeAssistant is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *HomeAssistant) String() string {
	var builder strings.Builder
	builder.WriteString("HomeAssistant(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteByte(')')
	return builder.String()
}

// HomeAssistants is a parsable slice of HomeAssistant.
type HomeAssistants []*HomeAssistant
