// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/aisettings"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AISettings is the model entity for the AISettings schema.
type AISettings struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider string `json:"provider,omitempty"`
	// APIKey holds the value of the "api_key" field.
	APIKey string `json:"api_key,omitempty"`
	// Model holds the value of the "model" field.
	Model string `json:"model,omitempty"`
	// Endpoint holds the value of the "endpoint" field.
	Endpoint                     string `json:"endpoint,omitempty"`
	general_settings_ai_settings *int
	selectValues                 sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AISettings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aisettings.FieldID:
			values[i] = new(sql.NullInt64)
		case aisettings.FieldProvider, aisettings.FieldAPIKey, aisettings.FieldModel, aisettings.FieldEndpoint:
			values[i] = new(sql.NullString)
		case aisettings.ForeignKeys[0]: // general_settings_ai_settings
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AISettings fields.
func (_m *AISettings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aisettings.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case aisettings.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = value.String
			}
		case aisettings.FieldAPIKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_key", values[i])
			} else if value.Valid {
				_m.APIKey = value.String
			}
		case aisettings.FieldModel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field model", values[i])
			} else if value.Valid {
				_m.Model = value.String
			}
		case aisettings.FieldEndpoint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field endpoint", values[i])
			} else if value.Valid {
				_m.Endpoint = value.String
			}
		case aisettings.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_ai_settings", value)
			} else if value.Valid {
				_m.general_settings_ai_settings = new(int)
				*_m.general_settings_ai_settings = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AISettings.
// This includes values selected through modifiers, order, etc.
func (_m *AISettings) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AISettings.
// Note that you need to call AISettings.Unwrap() before calling this method if this AISettings
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AISettings) Update() *AISettingsUpdateOne {
	return NewAISettingsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AISettings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AISettings) Unwrap() *AISettings {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AISettings is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AISettings) String() string {
	var builder strings.Builder
	builder.WriteString("AISettings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("provider=")
	builder.WriteString(_m.Provider)
	builder.WriteString(", ")
	builder.WriteString("api_key=")
	builder.WriteString(_m.APIKey)
	builder.WriteString(", ")
	builder.WriteString("model=")
	builder.WriteString(_m.Model)
	builder.WriteString(", ")
	builder.WriteString("endpoint=")
	builder.WriteString(_m.Endpoint)
	builder.WriteByte(')')
	return builder.String()
}

// AISettingsSlice is a parsable slice of AISettings.
type AISettingsSlice []*AISettings
