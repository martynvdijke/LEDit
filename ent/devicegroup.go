// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/devicegroup"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DeviceGroup is the model entity for the DeviceGroup schema.
type DeviceGroup struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// ContentMode holds the value of the "content_mode" field.
	ContentMode string `json:"content_mode,omitempty"`
	// PlaylistID holds the value of the "playlist_id" field.
	PlaylistID *int `json:"playlist_id,omitempty"`
	// ScheduledPlaylistIds holds the value of the "scheduled_playlist_ids" field.
	ScheduledPlaylistIds string `json:"scheduled_playlist_ids,omitempty"`
	// FallbackPlaylistID holds the value of the "fallback_playlist_id" field.
	FallbackPlaylistID *int `json:"fallback_playlist_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceGroupQuery when eager-loading is set.
	Edges        DeviceGroupEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeviceGroupEdges holds the relations/edges for other nodes in the graph.
type DeviceGroupEdges struct {
	// Devices holds the value of the devices edge.
	Devices []*DeviceSettings `json:"devices,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DevicesOrErr returns the Devices value or an error if the edge
// was not loaded in eager-loading.
func (e DeviceGroupEdges) DevicesOrErr() ([]*DeviceSettings, error) {
	if e.loadedTypes[0] {
		return e.Devices, nil
	}
	return nil, &NotLoadedError{edge: "devices"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case devicegroup.FieldID, devicegroup.FieldPlaylistID, devicegroup.FieldFallbackPlaylistID:
			values[i] = new(sql.NullInt64)
		case devicegroup.FieldName, devicegroup.FieldDescription, devicegroup.FieldContentMode, devicegroup.FieldScheduledPlaylistIds:
			values[i] = new(sql.NullString)
		case devicegroup.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceGroup fields.
func (_m *DeviceGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case devicegroup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case devicegroup.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case devicegroup.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case devicegroup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case devicegroup.FieldContentMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content_mode", values[i])
			} else if value.Valid {
				_m.ContentMode = value.String
			}
		case devicegroup.FieldPlaylistID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field playlist_id", values[i])
			} else if value.Valid {
				_m.PlaylistID = new(int)
				*_m.PlaylistID = int(value.Int64)
			}
		case devicegroup.FieldScheduledPlaylistIds:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scheduled_playlist_ids", values[i])
			} else if value.Valid {
				_m.ScheduledPlaylistIds = value.String
			}
		case devicegroup.FieldFallbackPlaylistID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field fallback_playlist_id", values[i])
			} else if value.Valid {
				_m.FallbackPlaylistID = new(int)
				*_m.FallbackPlaylistID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceGroup.
// This includes values selected through modifiers, order, etc.
func (_m *DeviceGroup) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDevices queries the "devices" edge of the DeviceGroup entity.
func (_m *DeviceGroup) QueryDevices() *DeviceSettingsQuery {
	return NewDeviceGroupClient(_m.config).QueryDevices(_m)
}

// Update returns a builder for updating this DeviceGroup.
// Note that you need to call DeviceGroup.Unwrap() before calling this method if this DeviceGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DeviceGroup) Update() *DeviceGroupUpdateOne {
	return NewDeviceGroupClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DeviceGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DeviceGroup) Unwrap() *DeviceGroup {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeviceGroup is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DeviceGroup) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceGroup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("content_mode=")
	builder.WriteString(_m.ContentMode)
	builder.WriteString(", ")
	if v := _m.PlaylistID; v != nil {
		builder.WriteString("playlist_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("scheduled_playlist_ids=")
	builder.WriteString(_m.ScheduledPlaylistIds)
	builder.WriteString(", ")
	if v := _m.FallbackPlaylistID; v != nil {
		builder.WriteString("fallback_playlist_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// DeviceGroups is a parsable slice of DeviceGroup.
type DeviceGroups []*DeviceGroup
