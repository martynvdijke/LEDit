// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/qrcode"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Qrcode is the model entity for the Qrcode schema.
type Qrcode struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Content holds the value of the "content" field.
	Content string `json:"content,omitempty"`
	// Mode holds the value of the "mode" field.
	Mode qrcode.Mode `json:"mode,omitempty"`
	// WifiSsid holds the value of the "wifi_ssid" field.
	WifiSsid string `json:"wifi_ssid,omitempty"`
	// WifiPassword holds the value of the "wifi_password" field.
	WifiPassword string `json:"wifi_password,omitempty"`
	// WifiAuth holds the value of the "wifi_auth" field.
	WifiAuth qrcode.WifiAuth `json:"wifi_auth,omitempty"`
	// Caption holds the value of the "caption" field.
	Caption string `json:"caption,omitempty"`
	// ErrorCorrection holds the value of the "error_correction" field.
	ErrorCorrection qrcode.ErrorCorrection `json:"error_correction,omitempty"`
	// QuietZone holds the value of the "quiet_zone" field.
	QuietZone                int `json:"quiet_zone,omitempty"`
	general_settings_qrcodes *int
	selectValues             sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Qrcode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case qrcode.FieldID, qrcode.FieldQuietZone:
			values[i] = new(sql.NullInt64)
		case qrcode.FieldContent, qrcode.FieldMode, qrcode.FieldWifiSsid, qrcode.FieldWifiPassword, qrcode.FieldWifiAuth, qrcode.FieldCaption, qrcode.FieldErrorCorrection:
			values[i] = new(sql.NullString)
		case qrcode.ForeignKeys[0]: // general_settings_qrcodes
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Qrcode fields.
func (_m *Qrcode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case qrcode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case qrcode.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				_m.Content = value.String
			}
		case qrcode.FieldMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mode", values[i])
			} else if value.Valid {
				_m.Mode = qrcode.Mode(value.String)
			}
		case qrcode.FieldWifiSsid:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field wifi_ssid", values[i])
			} else if value.Valid {
				_m.WifiSsid = value.String
			}
		case qrcode.FieldWifiPassword:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field wifi_password", values[i])
			} else if value.Valid {
				_m.WifiPassword = value.String
			}
		case qrcode.FieldWifiAuth:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field wifi_auth", values[i])
			} else if value.Valid {
				_m.WifiAuth = qrcode.WifiAuth(value.String)
			}
		case qrcode.FieldCaption:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field caption", values[i])
			} else if value.Valid {
				_m.Caption = value.String
			}
		case qrcode.FieldErrorCorrection:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_correction", values[i])
			} else if value.Valid {
				_m.ErrorCorrection = qrcode.ErrorCorrection(value.String)
			}
		case qrcode.FieldQuietZone:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quiet_zone", values[i])
			} else if value.Valid {
				_m.QuietZone = int(value.Int64)
			}
		case qrcode.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_qrcodes", value)
			} else if value.Valid {
				_m.general_settings_qrcodes = new(int)
				*_m.general_settings_qrcodes = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Qrcode.
// This includes values selected through modifiers, order, etc.
func (_m *Qrcode) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Qrcode.
// Note that you need to call Qrcode.Unwrap() before calling this method if this Qrcode
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Qrcode) Update() *QrcodeUpdateOne {
	return NewQrcodeClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Qrcode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Qrcode) Unwrap() *Qrcode {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Qrcode is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Qrcode) String() string {
	var builder strings.Builder
	builder.WriteString("Qrcode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("content=")
	builder.WriteString(_m.Content)
	builder.WriteString(", ")
	builder.WriteString("mode=")
	builder.WriteString(fmt.Sprintf("%v", _m.Mode))
	builder.WriteString(", ")
	builder.WriteString("wifi_ssid=")
	builder.WriteString(_m.WifiSsid)
	builder.WriteString(", ")
	builder.WriteString("wifi_password=")
	builder.WriteString(_m.WifiPassword)
	builder.WriteString(", ")
	builder.WriteString("wifi_auth=")
	builder.WriteString(fmt.Sprintf("%v", _m.WifiAuth))
	builder.WriteString(", ")
	builder.WriteString("caption=")
	builder.WriteString(_m.Caption)
	builder.WriteString(", ")
	builder.WriteString("error_correction=")
	builder.WriteString(fmt.Sprintf("%v", _m.ErrorCorrection))
	builder.WriteString(", ")
	builder.WriteString("quiet_zone=")
	builder.WriteString(fmt.Sprintf("%v", _m.QuietZone))
	builder.WriteByte(')')
	return builder.String()
}

// Qrcodes is a parsable slice of Qrcode.
type Qrcodes []*Qrcode
