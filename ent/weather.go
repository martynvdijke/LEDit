// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/weather"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Weather is the model entity for the Weather schema.
type Weather struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Token holds the value of the "token" field.
	Token string `json:"token,omitempty"`
	// URL holds the value of the "url" field.
	URL                      string `json:"url,omitempty"`
	general_settings_weather *int
	selectValues             sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Weather) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case weather.FieldID:
			values[i] = new(sql.NullInt64)
		case weather.FieldToken, weather.FieldURL:
			values[i] = new(sql.NullString)
		case weather.ForeignKeys[0]: // general_settings_weather
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Weather fields.
func (_m *Weather) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case weather.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case weather.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case weather.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case weather.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_weather", value)
			} else if value.Valid {
				_m.general_settings_weather = new(int)
				*_m.general_settings_weather = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Weather.
// This includes values selected through modifiers, order, etc.
func (_m *Weather) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Weather.
// Note that you need to call Weather.Unwrap() before calling this method if this Weather
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Weather) Update() *WeatherUpdateOne {
	return NewWeatherClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Weather entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Weather) Unwrap() *Weather {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Weather is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Weather) String() string {
	var builder strings.Builder
	builder.WriteString("Weather(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteByte(')')
	return builder.String()
}

// Weathers is a parsable slice of Weather.
type Weathers []*Weather
