// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/generalsettings"
	"ledit/ent/scene"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Scene is the model entity for the Scene schema.
type Scene struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled bool `json:"enabled,omitempty"`
	// Triggers holds the value of the "triggers" field.
	Triggers string `json:"triggers,omitempty"`
	// Actions holds the value of the "actions" field.
	Actions string `json:"actions,omitempty"`
	// Priority holds the value of the "priority" field.
	Priority int `json:"priority,omitempty"`
	// TTLSeconds holds the value of the "ttl_seconds" field.
	TTLSeconds *int `json:"ttl_seconds,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SceneQuery when eager-loading is set.
	Edges                   SceneEdges `json:"edges"`
	general_settings_scenes *int
	selectValues            sql.SelectValues
}

// SceneEdges holds the relations/edges for other nodes in the graph.
type SceneEdges struct {
	// GeneralSettings holds the value of the general_settings edge.
	GeneralSettings *GeneralSettings `json:"general_settings,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// GeneralSettingsOrErr returns the GeneralSettings value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SceneEdges) GeneralSettingsOrErr() (*GeneralSettings, error) {
	if e.GeneralSettings != nil {
		return e.GeneralSettings, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: generalsettings.Label}
	}
	return nil, &NotLoadedError{edge: "general_settings"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Scene) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case scene.FieldEnabled:
			values[i] = new(sql.NullBool)
		case scene.FieldID, scene.FieldPriority, scene.FieldTTLSeconds:
			values[i] = new(sql.NullInt64)
		case scene.FieldName, scene.FieldTriggers, scene.FieldActions:
			values[i] = new(sql.NullString)
		case scene.FieldCreatedAt, scene.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case scene.ForeignKeys[0]: // general_settings_scenes
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Scene fields.
func (_m *Scene) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case scene.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case scene.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case scene.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case scene.FieldTriggers:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field triggers", values[i])
			} else if value.Valid {
				_m.Triggers = value.String
			}
		case scene.FieldActions:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field actions", values[i])
			} else if value.Valid {
				_m.Actions = value.String
			}
		case scene.FieldPriority:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field priority", values[i])
			} else if value.Valid {
				_m.Priority = int(value.Int64)
			}
		case scene.FieldTTLSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ttl_seconds", values[i])
			} else if value.Valid {
				_m.TTLSeconds = new(int)
				*_m.TTLSeconds = int(value.Int64)
			}
		case scene.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case scene.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case scene.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_scenes", value)
			} else if value.Valid {
				_m.general_settings_scenes = new(int)
				*_m.general_settings_scenes = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Scene.
// This includes values selected through modifiers, order, etc.
func (_m *Scene) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGeneralSettings queries the "general_settings" edge of the Scene entity.
func (_m *Scene) QueryGeneralSettings() *GeneralSettingsQuery {
	return NewSceneClient(_m.config).QueryGeneralSettings(_m)
}

// Update returns a builder for updating this Scene.
// Note that you need to call Scene.Unwrap() before calling this method if this Scene
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Scene) Update() *SceneUpdateOne {
	return NewSceneClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Scene entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Scene) Unwrap() *Scene {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Scene is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Scene) String() string {
	var builder strings.Builder
	builder.WriteString("Scene(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteString(", ")
	builder.WriteString("triggers=")
	builder.WriteString(_m.Triggers)
	builder.WriteString(", ")
	builder.WriteString("actions=")
	builder.WriteString(_m.Actions)
	builder.WriteString(", ")
	builder.WriteString("priority=")
	builder.WriteString(fmt.Sprintf("%v", _m.Priority))
	builder.WriteString(", ")
	if v := _m.TTLSeconds; v != nil {
		builder.WriteString("ttl_seconds=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Scenes is a parsable slice of Scene.
type Scenes []*Scene
