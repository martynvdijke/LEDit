// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/aidigest"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AIDigest is the model entity for the AIDigest schema.
type AIDigest struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Prompt holds the value of the "prompt" field.
	Prompt string `json:"prompt,omitempty"`
	// Sources holds the value of the "sources" field.
	Sources string `json:"sources,omitempty"`
	// TTLMinutes holds the value of the "ttl_minutes" field.
	TTLMinutes int `json:"ttl_minutes,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled                     bool `json:"enabled,omitempty"`
	general_settings_ai_digests *int
	selectValues                sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AIDigest) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aidigest.FieldEnabled:
			values[i] = new(sql.NullBool)
		case aidigest.FieldID, aidigest.FieldTTLMinutes:
			values[i] = new(sql.NullInt64)
		case aidigest.FieldName, aidigest.FieldPrompt, aidigest.FieldSources:
			values[i] = new(sql.NullString)
		case aidigest.ForeignKeys[0]: // general_settings_ai_digests
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AIDigest fields.
func (_m *AIDigest) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aidigest.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case aidigest.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case aidigest.FieldPrompt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field prompt", values[i])
			} else if value.Valid {
				_m.Prompt = value.String
			}
		case aidigest.FieldSources:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sources", values[i])
			} else if value.Valid {
				_m.Sources = value.String
			}
		case aidigest.FieldTTLMinutes:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ttl_minutes", values[i])
			} else if value.Valid {
				_m.TTLMinutes = int(value.Int64)
			}
		case aidigest.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case aidigest.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_ai_digests", value)
			} else if value.Valid {
				_m.general_settings_ai_digests = new(int)
				*_m.general_settings_ai_digests = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AIDigest.
// This includes values selected through modifiers, order, etc.
func (_m *AIDigest) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AIDigest.
// Note that you need to call AIDigest.Unwrap() before calling this method if this AIDigest
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AIDigest) Update() *AIDigestUpdateOne {
	return NewAIDigestClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AIDigest entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AIDigest) Unwrap() *AIDigest {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AIDigest is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AIDigest) String() string {
	var builder strings.Builder
	builder.WriteString("AIDigest(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("prompt=")
	builder.WriteString(_m.Prompt)
	builder.WriteString(", ")
	builder.WriteString("sources=")
	builder.WriteString(_m.Sources)
	builder.WriteString(", ")
	builder.WriteString("ttl_minutes=")
	builder.WriteString(fmt.Sprintf("%v", _m.TTLMinutes))
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteByte(')')
	return builder.String()
}

// AIDigests is a parsable slice of AIDigest.
type AIDigests []*AIDigest
