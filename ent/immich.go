// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/immich"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Immich is the model entity for the Immich schema.
type Immich struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Immich API key (x-api-key header)
	Token string `json:"token,omitempty"`
	// Immich base URL
	URL string `json:"url,omitempty"`
	// JSON config: mode/album/interval_seconds/slideshow
	Config                   string `json:"config,omitempty"`
	general_settings_immichs *int
	selectValues             sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Immich) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case immich.FieldID:
			values[i] = new(sql.NullInt64)
		case immich.FieldToken, immich.FieldURL, immich.FieldConfig:
			values[i] = new(sql.NullString)
		case immich.ForeignKeys[0]: // general_settings_immichs
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Immich fields.
func (_m *Immich) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case immich.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case immich.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case immich.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case immich.FieldConfig:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field config", values[i])
			} else if value.Valid {
				_m.Config = value.String
			}
		case immich.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_immichs", value)
			} else if value.Valid {
				_m.general_settings_immichs = new(int)
				*_m.general_settings_immichs = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Immich.
// This includes values selected through modifiers, order, etc.
func (_m *Immich) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Immich.
// Note that you need to call Immich.Unwrap() before calling this method if this Immich
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Immich) Update() *ImmichUpdateOne {
	return NewImmichClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Immich entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Immich) Unwrap() *Immich {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Immich is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Immich) String() string {
	var builder strings.Builder
	builder.WriteString("Immich(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteString(", ")
	builder.WriteString("config=")
	builder.WriteString(_m.Config)
	builder.WriteByte(')')
	return builder.String()
}

// Immiches is a parsable slice of Immich.
type Immiches []*Immich
