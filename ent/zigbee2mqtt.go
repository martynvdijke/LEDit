// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/zigbee2mqtt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Zigbee2MQTT is the model entity for the Zigbee2MQTT schema.
type Zigbee2MQTT struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Zigbee2MQTT API token
	Token string `json:"token,omitempty"`
	// Zigbee2MQTT devices API URL
	URL                           string `json:"url,omitempty"`
	general_settings_zigbee2mqtts *int
	selectValues                  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Zigbee2MQTT) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case zigbee2mqtt.FieldID:
			values[i] = new(sql.NullInt64)
		case zigbee2mqtt.FieldToken, zigbee2mqtt.FieldURL:
			values[i] = new(sql.NullString)
		case zigbee2mqtt.ForeignKeys[0]: // general_settings_zigbee2mqtts
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Zigbee2MQTT fields.
func (_m *Zigbee2MQTT) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case zigbee2mqtt.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case zigbee2mqtt.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case zigbee2mqtt.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case zigbee2mqtt.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_zigbee2mqtts", value)
			} else if value.Valid {
				_m.general_settings_zigbee2mqtts = new(int)
				*_m.general_settings_zigbee2mqtts = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Zigbee2MQTT.
// This includes values selected through modifiers, order, etc.
func (_m *Zigbee2MQTT) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Zigbee2MQTT.
// Note that you need to call Zigbee2MQTT.Unwrap() before calling this method if this Zigbee2MQTT
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Zigbee2MQTT) Update() *Zigbee2MQTTUpdateOne {
	return NewZigbee2MQTTClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Zigbee2MQTT entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Zigbee2MQTT) Unwrap() *Zigbee2MQTT {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Zigbee2MQTT is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Zigbee2MQTT) String() string {
	var builder strings.Builder
	builder.WriteString("Zigbee2MQTT(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteByte(')')
	return builder.String()
}

// Zigbee2MQTTs is a parsable slice of Zigbee2MQTT.
type Zigbee2MQTTs []*Zigbee2MQTT
