// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/countdown"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Countdown is the model entity for the Countdown schema.
type Countdown struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// TargetTime holds the value of the "target_time" field.
	TargetTime time.Time `json:"target_time,omitempty"`
	// Label holds the value of the "label" field.
	Label string `json:"label,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled                     bool `json:"enabled,omitempty"`
	general_settings_countdowns *int
	selectValues                sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Countdown) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case countdown.FieldEnabled:
			values[i] = new(sql.NullBool)
		case countdown.FieldID:
			values[i] = new(sql.NullInt64)
		case countdown.FieldName, countdown.FieldLabel:
			values[i] = new(sql.NullString)
		case countdown.FieldTargetTime:
			values[i] = new(sql.NullTime)
		case countdown.ForeignKeys[0]: // general_settings_countdowns
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Countdown fields.
func (_m *Countdown) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case countdown.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case countdown.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case countdown.FieldTargetTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field target_time", values[i])
			} else if value.Valid {
				_m.TargetTime = value.Time
			}
		case countdown.FieldLabel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value.Valid {
				_m.Label = value.String
			}
		case countdown.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case countdown.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_countdowns", value)
			} else if value.Valid {
				_m.general_settings_countdowns = new(int)
				*_m.general_settings_countdowns = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Countdown.
// This includes values selected through modifiers, order, etc.
func (_m *Countdown) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Countdown.
// Note that you need to call Countdown.Unwrap() before calling this method if this Countdown
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Countdown) Update() *CountdownUpdateOne {
	return NewCountdownClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Countdown entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Countdown) Unwrap() *Countdown {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Countdown is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Countdown) String() string {
	var builder strings.Builder
	builder.WriteString("Countdown(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("target_time=")
	builder.WriteString(_m.TargetTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("label=")
	builder.WriteString(_m.Label)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteByte(')')
	return builder.String()
}

// Countdowns is a parsable slice of Countdown.
type Countdowns []*Countdown
