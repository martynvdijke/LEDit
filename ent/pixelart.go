// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/pixelart"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PixelArt is the model entity for the PixelArt schema.
type PixelArt struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// GridWidth holds the value of the "grid_width" field.
	GridWidth int `json:"grid_width,omitempty"`
	// GridHeight holds the value of the "grid_height" field.
	GridHeight int `json:"grid_height,omitempty"`
	// JSON: palette + frames
	Frames string `json:"frames,omitempty"`
	// JSON: colorSlots, frameRules, overlays
	Bindings string `json:"bindings,omitempty"`
	// APIURL holds the value of the "api_url" field.
	APIURL string `json:"api_url,omitempty"`
	// APIToken holds the value of the "api_token" field.
	APIToken string `json:"api_token,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled                     bool `json:"enabled,omitempty"`
	general_settings_pixel_arts *int
	selectValues                sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PixelArt) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pixelart.FieldEnabled:
			values[i] = new(sql.NullBool)
		case pixelart.FieldID, pixelart.FieldGridWidth, pixelart.FieldGridHeight:
			values[i] = new(sql.NullInt64)
		case pixelart.FieldName, pixelart.FieldFrames, pixelart.FieldBindings, pixelart.FieldAPIURL, pixelart.FieldAPIToken:
			values[i] = new(sql.NullString)
		case pixelart.ForeignKeys[0]: // general_settings_pixel_arts
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PixelArt fields.
func (_m *PixelArt) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pixelart.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case pixelart.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case pixelart.FieldGridWidth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field grid_width", values[i])
			} else if value.Valid {
				_m.GridWidth = int(value.Int64)
			}
		case pixelart.FieldGridHeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field grid_height", values[i])
			} else if value.Valid {
				_m.GridHeight = int(value.Int64)
			}
		case pixelart.FieldFrames:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field frames", values[i])
			} else if value.Valid {
				_m.Frames = value.String
			}
		case pixelart.FieldBindings:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field bindings", values[i])
			} else if value.Valid {
				_m.Bindings = value.String
			}
		case pixelart.FieldAPIURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_url", values[i])
			} else if value.Valid {
				_m.APIURL = value.String
			}
		case pixelart.FieldAPIToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_token", values[i])
			} else if value.Valid {
				_m.APIToken = value.String
			}
		case pixelart.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case pixelart.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_pixel_arts", value)
			} else if value.Valid {
				_m.general_settings_pixel_arts = new(int)
				*_m.general_settings_pixel_arts = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PixelArt.
// This includes values selected through modifiers, order, etc.
func (_m *PixelArt) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PixelArt.
// Note that you need to call PixelArt.Unwrap() before calling this method if this PixelArt
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PixelArt) Update() *PixelArtUpdateOne {
	return NewPixelArtClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PixelArt entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PixelArt) Unwrap() *PixelArt {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PixelArt is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PixelArt) String() string {
	var builder strings.Builder
	builder.WriteString("PixelArt(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("grid_width=")
	builder.WriteString(fmt.Sprintf("%v", _m.GridWidth))
	builder.WriteString(", ")
	builder.WriteString("grid_height=")
	builder.WriteString(fmt.Sprintf("%v", _m.GridHeight))
	builder.WriteString(", ")
	builder.WriteString("frames=")
	builder.WriteString(_m.Frames)
	builder.WriteString(", ")
	builder.WriteString("bindings=")
	builder.WriteString(_m.Bindings)
	builder.WriteString(", ")
	builder.WriteString("api_url=")
	builder.WriteString(_m.APIURL)
	builder.WriteString(", ")
	builder.WriteString("api_token=")
	builder.WriteString(_m.APIToken)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteByte(')')
	return builder.String()
}

// PixelArts is a parsable slice of PixelArt.
type PixelArts []*PixelArt
