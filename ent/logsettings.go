// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/logsettings"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// LogSettings is the model entity for the LogSettings schema.
type LogSettings struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Verbosity holds the value of the "verbosity" field.
	Verbosity string `json:"verbosity,omitempty"`
	// RetentionDays holds the value of the "retention_days" field.
	RetentionDays int `json:"retention_days,omitempty"`
	// OtelEndpoint holds the value of the "otel_endpoint" field.
	OtelEndpoint string `json:"otel_endpoint,omitempty"`
	// OtelProtocol holds the value of the "otel_protocol" field.
	OtelProtocol string `json:"otel_protocol,omitempty"`
	// OtelEnabled holds the value of the "otel_enabled" field.
	OtelEnabled  bool `json:"otel_enabled,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LogSettings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case logsettings.FieldOtelEnabled:
			values[i] = new(sql.NullBool)
		case logsettings.FieldID, logsettings.FieldRetentionDays:
			values[i] = new(sql.NullInt64)
		case logsettings.FieldVerbosity, logsettings.FieldOtelEndpoint, logsettings.FieldOtelProtocol:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LogSettings fields.
func (_m *LogSettings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case logsettings.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case logsettings.FieldVerbosity:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field verbosity", values[i])
			} else if value.Valid {
				_m.Verbosity = value.String
			}
		case logsettings.FieldRetentionDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field retention_days", values[i])
			} else if value.Valid {
				_m.RetentionDays = int(value.Int64)
			}
		case logsettings.FieldOtelEndpoint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field otel_endpoint", values[i])
			} else if value.Valid {
				_m.OtelEndpoint = value.String
			}
		case logsettings.FieldOtelProtocol:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field otel_protocol", values[i])
			} else if value.Valid {
				_m.OtelProtocol = value.String
			}
		case logsettings.FieldOtelEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field otel_enabled", values[i])
			} else if value.Valid {
				_m.OtelEnabled = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LogSettings.
// This includes values selected through modifiers, order, etc.
func (_m *LogSettings) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this LogSettings.
// Note that you need to call LogSettings.Unwrap() before calling this method if this LogSettings
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *LogSettings) Update() *LogSettingsUpdateOne {
	return NewLogSettingsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the LogSettings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *LogSettings) Unwrap() *LogSettings {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: LogSettings is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *LogSettings) String() string {
	var builder strings.Builder
	builder.WriteString("LogSettings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("verbosity=")
	builder.WriteString(_m.Verbosity)
	builder.WriteString(", ")
	builder.WriteString("retention_days=")
	builder.WriteString(fmt.Sprintf("%v", _m.RetentionDays))
	builder.WriteString(", ")
	builder.WriteString("otel_endpoint=")
	builder.WriteString(_m.OtelEndpoint)
	builder.WriteString(", ")
	builder.WriteString("otel_protocol=")
	builder.WriteString(_m.OtelProtocol)
	builder.WriteString(", ")
	builder.WriteString("otel_enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.OtelEnabled))
	builder.WriteByte(')')
	return builder.String()
}

// LogSettingsSlice is a parsable slice of LogSettings.
type LogSettingsSlice []*LogSettings
