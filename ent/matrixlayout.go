// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"ledit/ent/matrixlayout"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// MatrixLayout is the model entity for the MatrixLayout schema.
type MatrixLayout struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Rows holds the value of the "rows" field.
	Rows int `json:"rows,omitempty"`
	// Cols holds the value of the "cols" field.
	Cols int `json:"cols,omitempty"`
	// Gap holds the value of the "gap" field.
	Gap int `json:"gap,omitempty"`
	// Background holds the value of the "background" field.
	Background string `json:"background,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled bool `json:"enabled,omitempty"`
	// JSON array of {row, col, source_type, source_id}
	Bindings                        string `json:"bindings,omitempty"`
	general_settings_matrix_layouts *int
	selectValues                    sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MatrixLayout) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case matrixlayout.FieldEnabled:
			values[i] = new(sql.NullBool)
		case matrixlayout.FieldID, matrixlayout.FieldRows, matrixlayout.FieldCols, matrixlayout.FieldGap:
			values[i] = new(sql.NullInt64)
		case matrixlayout.FieldName, matrixlayout.FieldBackground, matrixlayout.FieldBindings:
			values[i] = new(sql.NullString)
		case matrixlayout.ForeignKeys[0]: // general_settings_matrix_layouts
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MatrixLayout fields.
func (_m *MatrixLayout) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case matrixlayout.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case matrixlayout.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case matrixlayout.FieldRows:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field rows", values[i])
			} else if value.Valid {
				_m.Rows = int(value.Int64)
			}
		case matrixlayout.FieldCols:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field cols", values[i])
			} else if value.Valid {
				_m.Cols = int(value.Int64)
			}
		case matrixlayout.FieldGap:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field gap", values[i])
			} else if value.Valid {
				_m.Gap = int(value.Int64)
			}
		case matrixlayout.FieldBackground:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field background", values[i])
			} else if value.Valid {
				_m.Background = value.String
			}
		case matrixlayout.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case matrixlayout.FieldBindings:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field bindings", values[i])
			} else if value.Valid {
				_m.Bindings = value.String
			}
		case matrixlayout.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field general_settings_matrix_layouts", value)
			} else if value.Valid {
				_m.general_settings_matrix_layouts = new(int)
				*_m.general_settings_matrix_layouts = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MatrixLayout.
// This includes values selected through modifiers, order, etc.
func (_m *MatrixLayout) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this MatrixLayout.
// Note that you need to call MatrixLayout.Unwrap() before calling this method if this MatrixLayout
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MatrixLayout) Update() *MatrixLayoutUpdateOne {
	return NewMatrixLayoutClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MatrixLayout entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MatrixLayout) Unwrap() *MatrixLayout {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: MatrixLayout is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MatrixLayout) String() string {
	var builder strings.Builder
	builder.WriteString("MatrixLayout(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("rows=")
	builder.WriteString(fmt.Sprintf("%v", _m.Rows))
	builder.WriteString(", ")
	builder.WriteString("cols=")
	builder.WriteString(fmt.Sprintf("%v", _m.Cols))
	builder.WriteString(", ")
	builder.WriteString("gap=")
	builder.WriteString(fmt.Sprintf("%v", _m.Gap))
	builder.WriteString(", ")
	builder.WriteString("background=")
	builder.WriteString(_m.Background)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteString(", ")
	builder.WriteString("bindings=")
	builder.WriteString(_m.Bindings)
	builder.WriteByte(')')
	return builder.String()
}

// MatrixLayouts is a parsable slice of MatrixLayout.
type MatrixLayouts []*MatrixLayout
