// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ledit/ent/textslide"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TextSlideCreate is the builder for creating a TextSlide entity.
type TextSlideCreate struct {
	config
	mutation *TextSlideMutation
	hooks    []Hook
}

// SetContent sets the "content" field.
func (_c *TextSlideCreate) SetContent(v string) *TextSlideCreate {
	_c.mutation.SetContent(v)
	return _c
}

// SetColor sets the "color" field.
func (_c *TextSlideCreate) SetColor(v string) *TextSlideCreate {
	_c.mutation.SetColor(v)
	return _c
}

// SetNillableColor sets the "color" field if the given value is not nil.
func (_c *TextSlideCreate) SetNillableColor(v *string) *TextSlideCreate {
	if v != nil {
		_c.SetColor(*v)
	}
	return _c
}

// SetBgColor sets the "bg_color" field.
func (_c *TextSlideCreate) SetBgColor(v string) *TextSlideCreate {
	_c.mutation.SetBgColor(v)
	return _c
}

// SetNillableBgColor sets the "bg_color" field if the given value is not nil.
func (_c *TextSlideCreate) SetNillableBgColor(v *string) *TextSlideCreate {
	if v != nil {
		_c.SetBgColor(*v)
	}
	return _c
}

// SetFontSize sets the "font_size" field.
func (_c *TextSlideCreate) SetFontSize(v int) *TextSlideCreate {
	_c.mutation.SetFontSize(v)
	return _c
}

// SetNillableFontSize sets the "font_size" field if the given value is not nil.
func (_c *TextSlideCreate) SetNillableFontSize(v *int) *TextSlideCreate {
	if v != nil {
		_c.SetFontSize(*v)
	}
	return _c
}

// Mutation returns the TextSlideMutation object of the builder.
func (_c *TextSlideCreate) Mutation() *TextSlideMutation {
	return _c.mutation
}

// Save creates the TextSlide in the database.
func (_c *TextSlideCreate) Save(ctx context.Context) (*TextSlide, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *TextSlideCreate) SaveX(ctx context.Context) *TextSlide {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *TextSlideCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *TextSlideCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *TextSlideCreate) defaults() {
	if _, ok := _c.mutation.Color(); !ok {
		v := textslide.DefaultColor
		_c.mutation.SetColor(v)
	}
	if _, ok := _c.mutation.BgColor(); !ok {
		v := textslide.DefaultBgColor
		_c.mutation.SetBgColor(v)
	}
	if _, ok := _c.mutation.FontSize(); !ok {
		v := textslide.DefaultFontSize
		_c.mutation.SetFontSize(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *TextSlideCreate) check() error {
	if _, ok := _c.mutation.Content(); !ok {
		return &ValidationError{Name: "content", err: errors.New(`ent: missing required field "TextSlide.content"`)}
	}
	if _, ok := _c.mutation.Color(); !ok {
		return &ValidationError{Name: "color", err: errors.New(`ent: missing required field "TextSlide.color"`)}
	}
	if _, ok := _c.mutation.BgColor(); !ok {
		return &ValidationError{Name: "bg_color", err: errors.New(`ent: missing required field "TextSlide.bg_color"`)}
	}
	if _, ok := _c.mutation.FontSize(); !ok {
		return &ValidationError{Name: "font_size", err: errors.New(`ent: missing required field "TextSlide.font_size"`)}
	}
	return nil
}

func (_c *TextSlideCreate) sqlSave(ctx context.Context) (*TextSlide, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *TextSlideCreate) createSpec() (*TextSlide, *sqlgraph.CreateSpec) {
	var (
		_node = &TextSlide{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(textslide.Table, sqlgraph.NewFieldSpec(textslide.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.Content(); ok {
		_spec.SetField(textslide.FieldContent, field.TypeString, value)
		_node.Content = value
	}
	if value, ok := _c.mutation.Color(); ok {
		_spec.SetField(textslide.FieldColor, field.TypeString, value)
		_node.Color = value
	}
	if value, ok := _c.mutation.BgColor(); ok {
		_spec.SetField(textslide.FieldBgColor, field.TypeString, value)
		_node.BgColor = value
	}
	if value, ok := _c.mutation.FontSize(); ok {
		_spec.SetField(textslide.FieldFontSize, field.TypeInt, value)
		_node.FontSize = value
	}
	return _node, _spec
}

// TextSlideCreateBulk is the builder for creating many TextSlide entities in bulk.
type TextSlideCreateBulk struct {
	config
	err      error
	builders []*TextSlideCreate
}

// Save creates the TextSlide entities in the database.
func (_c *TextSlideCreateBulk) Save(ctx context.Context) ([]*TextSlide, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*TextSlide, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TextSlideMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *TextSlideCreateBulk) SaveX(ctx context.Context) []*TextSlide {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *TextSlideCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *TextSlideCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
