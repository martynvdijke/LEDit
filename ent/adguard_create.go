// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ledit/ent/adguard"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AdGuardCreate is the builder for creating a AdGuard entity.
type AdGuardCreate struct {
	config
	mutation *AdGuardMutation
	hooks    []Hook
}

// SetToken sets the "token" field.
func (_c *AdGuardCreate) SetToken(v string) *AdGuardCreate {
	_c.mutation.SetToken(v)
	return _c
}

// SetNillableToken sets the "token" field if the given value is not nil.
func (_c *AdGuardCreate) SetNillableToken(v *string) *AdGuardCreate {
	if v != nil {
		_c.SetToken(*v)
	}
	return _c
}

// SetURL sets the "url" field.
func (_c *AdGuardCreate) SetURL(v string) *AdGuardCreate {
	_c.mutation.SetURL(v)
	return _c
}

// SetNillableURL sets the "url" field if the given value is not nil.
func (_c *AdGuardCreate) SetNillableURL(v *string) *AdGuardCreate {
	if v != nil {
		_c.SetURL(*v)
	}
	return _c
}

// Mutation returns the AdGuardMutation object of the builder.
func (_c *AdGuardCreate) Mutation() *AdGuardMutation {
	return _c.mutation
}

// Save creates the AdGuard in the database.
func (_c *AdGuardCreate) Save(ctx context.Context) (*AdGuard, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *AdGuardCreate) SaveX(ctx context.Context) *AdGuard {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *AdGuardCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *AdGuardCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *AdGuardCreate) defaults() {
	if _, ok := _c.mutation.Token(); !ok {
		v := adguard.DefaultToken
		_c.mutation.SetToken(v)
	}
	if _, ok := _c.mutation.URL(); !ok {
		v := adguard.DefaultURL
		_c.mutation.SetURL(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *AdGuardCreate) check() error {
	if _, ok := _c.mutation.Token(); !ok {
		return &ValidationError{Name: "token", err: errors.New(`ent: missing required field "AdGuard.token"`)}
	}
	if _, ok := _c.mutation.URL(); !ok {
		return &ValidationError{Name: "url", err: errors.New(`ent: missing required field "AdGuard.url"`)}
	}
	return nil
}

func (_c *AdGuardCreate) sqlSave(ctx context.Context) (*AdGuard, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *AdGuardCreate) createSpec() (*AdGuard, *sqlgraph.CreateSpec) {
	var (
		_node = &AdGuard{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(adguard.Table, sqlgraph.NewFieldSpec(adguard.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.Token(); ok {
		_spec.SetField(adguard.FieldToken, field.TypeString, value)
		_node.Token = value
	}
	if value, ok := _c.mutation.URL(); ok {
		_spec.SetField(adguard.FieldURL, field.TypeString, value)
		_node.URL = value
	}
	return _node, _spec
}

// AdGuardCreateBulk is the builder for creating many AdGuard entities in bulk.
type AdGuardCreateBulk struct {
	config
	err      error
	builders []*AdGuardCreate
}

// Save creates the AdGuard entities in the database.
func (_c *AdGuardCreateBulk) Save(ctx context.Context) ([]*AdGuard, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*AdGuard, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AdGuardMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *AdGuardCreateBulk) SaveX(ctx context.Context) []*AdGuard {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *AdGuardCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *AdGuardCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
